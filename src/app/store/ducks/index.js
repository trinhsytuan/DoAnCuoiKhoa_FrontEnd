import * as app from './app.duck';
import * as user from './user.duck';

export const DUCKS = {
  app,
  user,
  // orgUnit,
  // caiDat,
  // role,
  // notification,


  // extraData: extraField,
  // rerender,
};
